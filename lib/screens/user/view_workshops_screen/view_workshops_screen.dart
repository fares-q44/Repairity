import 'package:flutter/material.dart';

class ViewWorkshopScreen extends StatelessWidget {
  const ViewWorkshopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is view workshops screen'),
    );
  }
}
